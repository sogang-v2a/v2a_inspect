You are helping select an audio generation model by analyzing video segments visually.

Context for each segment is provided: scene index, time range, track kind, object count in scene, and sound description.

Models:
- VTA (Video-to-Audio): precise temporal alignment with visuals; weaker at separating multiple sources
- TTA (Text-to-Audio): superior sound quality and multi-source isolation; no temporal sync

Score each segment on 3 VIDEO-OBSERVABLE dimensions (1–5 integers):

motion_level — Visual dynamism of the segment
  5 = extremely fast motion, impacts, collisions, camera shake
  4 = active movement (running, quick gestures, fast vehicle)
  3 = moderate movement (walking, slow vehicle, gradual scene change)
  2 = mostly still with minor motion
  1 = completely static or imperceptibly slow

event_coupling — How directly do visual events trigger distinct individual sounds?
  5 = every visible action clearly produces a unique, isolatable sound (footstep, punch, machine click)
  4 = most visual events have clear sound correspondences
  3 = some visual-sound correspondence, but not all events are isolatable
  2 = loose relationship; sounds are more atmospheric than event-driven
  1 = purely diffuse ambient — no specific visual event triggers sounds

source_diversity — How many INDEPENDENT sound-producing subjects are simultaneously visible and active?
  5 = four or more distinct moving subjects each likely producing separate sounds
  4 = three distinct subjects
  3 = two distinct subjects
  2 = one primary subject with minor background activity
  1 = single unified soundscape, no independent sources

IMPORTANT: n_objects_in_scene tells you how many foreground objects the scene analysis found.
Use it as a strong hint for source_diversity.

Segments to evaluate:
{segment_list}

Watch the video at each indicated time range. Return ONLY valid JSON:
{{"segments": [{{"segment_index": 0, "motion_level": 3, "event_coupling": 4, "source_diversity": 2, "reasoning": "brief 1-sentence reason"}}]}}
